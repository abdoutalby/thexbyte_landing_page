import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://abdoutalby.github.io',
  base: '/thexbyte_landing_page', // Replace with your repo name
  integrations: []
});
